# 🚀 Быстрый старт на macOS

## За 2 минуты до первого запуска

### Шаг 1: Откройте Terminal

```bash
# Перейдите в папку проекта
cd /path/to/course-management
```

### Шаг 2: Запустите скрипт установки

```bash
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- ✅ Проверит Python
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Создаст необходимые папки

### Шаг 3: Запустите приложение

```bash
source venv/bin/activate
python app.py
```

Вы увидите:
```
 * Running on http://127.0.0.1:5000
 * Press CTRL+C to quit
```

### Шаг 4: Откройте браузер

Перейдите на **http://localhost:5000**

## 🎓 Первые действия в приложении

### 1. Добавьте сотрудника
```
Вкладка: 👥 Сотрудники
ФИО: Иван Иванов
Номер: +79991234567
Должность: Разработчик
Нажмите: ➕ Добавить сотрудника
```

### 2. Добавьте курс
```
Вкладка: 📖 Курсы
Название: Python для начинающих
Длительность: 40 часов
Документ: Сертификат
Нажмите: ➕ Добавить курс
```

### 3. Зачислите на курс
```
Вкладка: ✏️ Регистрации
Выберите: Иван Иванов
Выберите: Python для начинающих
Нажмите: ✅ Зачислить на курс
```

### 4. Обновите прогресс
```
На вкладке Регистрации найдите запись
Нажмите: 📊 Прогресс
Введите: 50
Статус изменится на "В процессе"
```

### 5. Завершите курс
```
Нажмите: ✅ Статус
Выберите: 2 (completed)
Запись перейдёт в "Завершен" с 100% прогресса
```

### 6. Смотрите отчёт
```
Вкладка: 📈 Отчёты
Смотрите: рекомендации
Скачайте: CSV отчёт
```

## 🧪 Запуск тестов

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите все тесты
pytest test_app.py -v

# Результат:
# test_app.py::test_create_employee PASSED
# test_app.py::test_create_course PASSED
# ... и ещё 33+ теста
```

## 🆘 Решение проблем

### Проблема: "Python3 не найден"
```bash
# Установите Python:
# https://www.python.org/downloads/
# Или через Homebrew:
brew install python3
```

### Проблема: "Port 5000 already in use"
```bash
# Найдите процесс:
lsof -i :5000

# Убейте его:
kill -9 <PID>

# Или используйте другой порт в app.py:
# app.run(port=5001)
```

### Проблема: "ModuleNotFoundError"
```bash
# Убедитесь, что виртуальное окружение активно
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: "Database is locked"
```bash
# Удалите базу данных
rm courses.db

# Приложение создаст новую при запуске
python app.py
```

## 📱 Доступ из других устройств в сети

```bash
# Вместо localhost используйте IP адрес:
# На Mac узнайте IP:
ifconfig | grep "inet " | grep -v 127.0.0.1

# Например: http://192.168.1.100:5000
```

## 🔌 Структура файлов после установки

```
course-management/
├── venv/                      # Виртуальное окружение
├── app.py                     # Основное приложение
├── reports.py                 # Генератор отчётов
├── test_app.py               # Тесты
├── config.py                 # Конфигурация
├── setup.sh                  # Скрипт установки
├── requirements.txt          # Зависимости
├── README.md                 # Полная документация
├── QUICKSTART.md            # Этот файл
├── templates/
│   └── index.html           # Главная страница
├── static/
│   └── app.js              # JavaScript логика
├── courses.db              # База данных (создаётся при первом запуске)
└── reports/                # Экспортированные отчёты
```

## ⚡ Полезные команды

```bash
# Активация окружения
source venv/bin/activate

# Деактивация окружения
deactivate

# Запуск приложения
python app.py

# Запуск тестов
pytest test_app.py -v

# Запуск с покрытием
pytest test_app.py --cov=app

# Проверка зависимостей
pip list

# Обновление зависимостей
pip install -r requirements.txt --upgrade

# Экспорт текущих зависимостей
pip freeze > requirements.txt

# Очистка кэша Python
find . -type d -name __pycache__ -exec rm -r {} +
find . -type f -name "*.pyc" -delete
```

## 📊 API примеры

### Через curl

```bash
# Добавить сотрудника
curl -X POST http://localhost:5000/api/employees \
  -H "Content-Type: application/json" \
  -d '{"full_name":"Иван Иванов","phone":"+79991234567","position":"Разработчик"}'

# Получить всех сотрудников
curl http://localhost:5000/api/employees

# Поиск по номеру
curl "http://localhost:5000/api/employees/search?q=79991234567"

# Получить статистику
curl http://localhost:5000/api/statistics

# Скачать отчёт
curl http://localhost:5000/api/report/generate -o report.csv
```

## 💡 Советы для разработки

### Режим отладки
```python
# В app.py
app.run(debug=True)  # Автоперезагрузка при изменениях
```

### Просмотр SQL запросов
```python
# В app.py измените:
app.config['SQLALCHEMY_ECHO'] = True
```

### Добавление новых должностей
```python
# В app.py найдите:
VALID_POSITIONS = ['Аналитик', 'Разработчик', ...]
# И добавьте свою
```

### Экспорт в другие форматы
```python
# В reports.py добавьте функции для:
# - JSON
# - XML
# - Excel
```

## 🎓 Учебные задачи

### Задача 1: Добавить новую должность
Отредактируйте `VALID_POSITIONS` в `app.py`

### Задача 2: Изменить цвета интерфейса
Отредактируйте `:root` переменные в `templates/index.html`

### Задача 3: Добавить новую метрику в отчёт
Отредактируйте функцию `generate_full_report` в `reports.py`

### Задача 4: Добавить новый тест
Добавьте функцию `test_*` в `test_app.py`

## 📞 Контакты и поддержка

При проблемах:
1. Проверьте README.md
2. Посмотрите логи в консоли
3. Запустите тесты: `pytest test_app.py -v`

## ✅ Чек-лист первого запуска

- [ ] Python 3.8+ установлен
- [ ] Папка проекта создана
- [ ] setup.sh выполнен успешно
- [ ] Виртуальное окружение активировано
- [ ] Приложение запущено на http://localhost:5000
- [ ] Браузер открывает главную страницу
- [ ] Добавлен первый сотрудник
- [ ] Добавлен первый курс
- [ ] Создана первая регистрация
- [ ] Тесты проходят успешно

**Готово к использованию!** 🎉
