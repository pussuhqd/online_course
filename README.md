# 📚 Система учёта прохождения онлайн-курсов для персонала

Полнофункциональная веб-система для управления корпоративным обучением персонала. Позволяет отслеживать прохождение онлайн-курсов, генерировать отчеты и получать рекомендации по улучшению.

## ✨ Основные возможности

### 1. **Управление сотрудниками**
- ✅ Добавление сотрудников (ФИО, номер телефона, должность)
- ✅ Валидация данных (ФИО, номер, должность из списка)
- ✅ Проверка на дубликаты
- ✅ Поиск по ФИО или номеру телефона
- ✅ Удаление записей (с сохранением истории)

### 2. **Управление курсами**
- ✅ Добавление курсов (название, длительность, тип документа)
- ✅ Валидация данных
- ✅ Поддержка типов документов: Диплом, Сертификат, Удостоверение, Свидетельство
- ✅ Уникальность названий курсов
- ✅ Удаление курсов

### 3. **Регистрация и отслеживание**
- ✅ Зачисление сотрудников на курсы
- ✅ Жизненный цикл курса: Зачислен → В процессе → Завершен
- ✅ Отслеживание прогресса (0-100%)
- ✅ Система с логикой (нельзя завершить, если не начат)
- ✅ История дат регистрации, начала и завершения

### 4. **Отчёты и аналитика**
- ✅ Дашборд со статистикой
- ✅ Общие метрики (сотрудники, курсы, регистрации)
- ✅ Распределение по статусам
- ✅ Топ популярных курсов
- ✅ Статистика по должностям
- ✅ Экспорт в CSV
- ✅ Интеллектуальные рекомендации

### 5. **Тестирование**
- ✅ 35+ unit тестов с pytest
- ✅ Тесты валидации
- ✅ Тесты жизненного цикла
- ✅ Интеграционные тесты

## 🛠️ Требования

- Python 3.8+
- macOS (полная совместимость)
- Windows/Linux также поддерживаются

## 📦 Установка

### 1. Клонируйте репозиторий или создайте папку проекта

```bash
mkdir course-management
cd course-management
```

### 2. Создайте виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # На macOS/Linux
# или
venv\Scripts\activate  # На Windows
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

## 🚀 Запуск приложения

### 1. Запустите Flask сервер

```bash
python app.py
```

Сервер запустится на `http://localhost:5000`

### 2. Откройте браузер

Перейдите на `http://localhost:5000`

## 🧪 Запуск тестов

```bash
# Запустить все тесты
pytest test_app.py -v

# Запустить с покрытием
pytest test_app.py --cov=app

# Запустить конкретный тест
pytest test_app.py::test_create_employee -v

# Запустить с подробным выводом
pytest test_app.py -vv
```

## 📁 Структура проекта

```
course-management/
├── app.py                 # Основной Flask файл (модели + API)
├── reports.py            # Генератор отчётов
├── test_app.py           # Unit тесты (35+ тестов)
├── requirements.txt      # Зависимости Python
├── templates/
│   └── index.html        # Главная страница (фронтенд)
├── static/
│   └── app.js           # JavaScript логика
├── courses.db           # База данных SQLite (создаётся автоматически)
└── reports/             # Папка с экспортированными отчётами
```

## 💻 Использование

### Добавление сотрудника

1. Перейдите на вкладку "👥 Сотрудники"
2. Заполните форму:
   - ФИО (минимум 3 символа)
   - Номер телефона (минимум 10 цифр)
   - Должность (из списка)
3. Нажмите "➕ Добавить сотрудника"

**Доступные должности:**
- Аналитик
- Разработчик
- Тестировщик
- Менеджер проекта
- Системный администратор
- Дизайнер
- Архитектор ПО

### Добавление курса

1. Перейдите на вкладку "📖 Курсы"
2. Заполните форму:
   - Название (минимум 3 символа)
   - Длительность в часах (1-10000)
   - Тип документа (Диплом, Сертификат и т.д.)
3. Нажмите "➕ Добавить курс"

### Зачисление на курс

1. Перейдите на вкладку "✏️ Регистрации"
2. Выберите сотрудника и курс
3. Нажмите "✅ Зачислить на курс"

### Обновление прогресса

1. На вкладке "✏️ Регистрации" найдите регистрацию
2. Нажмите "📊 Прогресс"
3. Введите значение 0-100
4. Статус автоматически изменится на "В процессе"

### Изменение статуса курса

1. На вкладке "✏️ Регистрации" найдите регистрацию
2. Нажмите "✅ Статус"
3. Выберите:
   - `1` для "В процессе"
   - `2` для "Завершен"

### Экспорт отчёта

1. Перейдите на вкладку "📈 Отчёты"
2. Посмотрите рекомендации
3. Нажмите "📊 Скачать CSV отчёт"

### Просмотр удаленных записей

1. Перейдите на вкладку "🗑️ Удаленные"
2. Посмотрите все удаленные сотрудников, курсы и регистрации

## 🎨 Дизайн и интерфейс

- **Современный градиентный дизайн** с фиолетовыми и голубыми оттенками
- **Отзывчивый интерфейс** (работает на мобильных, планшетах и ПК)
- **Интерактивные таблицы** с сортировкой и поиском
- **Визуальные индикаторы** прогресса
- **Темные и светлые темы** в зависимости от настроек системы

## 🔍 Валидация данных

### Сотрудники
- ✅ ФИО: 3+ символов, только буквы и пробелы
- ✅ Номер: минимум 10 цифр
- ✅ Должность: только из предустановленного списка
- ✅ Уникальность: не может быть дублей по номеру или ФИО+должность

### Курсы
- ✅ Название: 3+ символов, уникальное
- ✅ Длительность: 1-10000 часов
- ✅ Тип документа: только из списка

### Регистрации
- ✅ Сотрудник и курс существуют
- ✅ Нет дублирования (один сотрудник не может быть зачислен на один курс дважды)
- ✅ Логика жизненного цикла (нельзя перейти в completed без in_progress)

## 📊 Отчёты и рекомендации

Система автоматически генерирует рекомендации на основе:

1. **Низкий средний прогресс** - рекомендация увеличить поддержку
2. **Много неначатых курсов** - напомнить участникам начать
3. **Медленное завершение** - проверить сложность и сроки
4. **Низкое участие должностей** - создать специализированные курсы
5. **Популярные курсы** - рассмотреть расширение
6. **Высокий уровень завершения** - поздравление

## 📈 Структура базы данных

### Таблица: employees
```
- id (INTEGER PRIMARY KEY)
- full_name (STRING)
- phone (STRING UNIQUE)
- position (STRING)
- created_at (DATETIME)
- is_deleted (BOOLEAN)
- deleted_at (DATETIME)
```

### Таблица: courses
```
- id (INTEGER PRIMARY KEY)
- title (STRING UNIQUE)
- duration_hours (INTEGER)
- certificate_type (STRING)
- created_at (DATETIME)
- is_deleted (BOOLEAN)
- deleted_at (DATETIME)
```

### Таблица: registrations
```
- id (INTEGER PRIMARY KEY)
- employee_id (FOREIGN KEY)
- course_id (FOREIGN KEY)
- status (STRING) - enrolled, in_progress, completed
- progress_percent (INTEGER)
- registered_at (DATETIME)
- started_at (DATETIME)
- completed_at (DATETIME)
- is_deleted (BOOLEAN)
- deleted_at (DATETIME)
```

## 🔧 API Endpoints

### Сотрудники
- `GET /api/employees` - Получить всех сотрудников
- `POST /api/employees` - Добавить сотрудника
- `GET /api/employees/<id>` - Получить сотрудника по ID
- `GET /api/employees/search?q=...` - Поиск по ФИО/телефону
- `DELETE /api/employees/<id>` - Удалить сотрудника

### Курсы
- `GET /api/courses` - Получить все курсы
- `POST /api/courses` - Добавить курс
- `GET /api/courses/<id>` - Получить курс по ID
- `DELETE /api/courses/<id>` - Удалить курс

### Регистрации
- `GET /api/registrations` - Получить все регистрации
- `POST /api/registrations` - Добавить регистрацию
- `PUT /api/registrations/<id>/status` - Изменить статус
- `PUT /api/registrations/<id>/progress` - Обновить прогресс
- `DELETE /api/registrations/<id>` - Удалить регистрацию

### Статистика и отчёты
- `GET /api/statistics` - Получить статистику
- `GET /api/report/recommendations` - Получить рекомендации
- `GET /api/report/generate` - Скачать CSV отчёт

## 🧪 Примеры тестов

```bash
# Все тесты
pytest test_app.py -v

# Только тесты сотрудников
pytest test_app.py -k employee -v

# Только тесты регистраций
pytest test_app.py -k registration -v

# Показать покрытие
pytest test_app.py --cov=app --cov-report=html
```

## 📝 Примеры cURL запросов

### Добавить сотрудника
```bash
curl -X POST http://localhost:5000/api/employees \
  -H "Content-Type: application/json" \
  -d '{
    "full_name": "Иван Иванов",
    "phone": "+79991234567",
    "position": "Разработчик"
  }'
```

### Добавить курс
```bash
curl -X POST http://localhost:5000/api/courses \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Python для начинающих",
    "duration_hours": 40,
    "certificate_type": "Сертификат"
  }'
```

### Зачислить на курс
```bash
curl -X POST http://localhost:5000/api/registrations \
  -H "Content-Type: application/json" \
  -d '{
    "employee_id": 1,
    "course_id": 1
  }'
```

### Обновить статус
```bash
curl -X PUT http://localhost:5000/api/registrations/1/status \
  -H "Content-Type: application/json" \
  -d '{"status": "in_progress"}'
```

## 🐛 Отладка

### Если приложение не запускается

1. Убедитесь, что виртуальное окружение активировано
2. Проверьте, установлены ли все зависимости: `pip list`
3. Убедитесь, что порт 5000 не занят
4. Проверьте, что вы находитесь в правильной директории

### Если тесты не запускаются

1. Установите pytest: `pip install pytest pytest-cov`
2. Убедитесь, что находитесь в корневой папке проекта
3. Проверьте, что файл `test_app.py` существует

### Если база данных повреждена

```bash
# Удалите базу данных
rm courses.db

# Приложение создаст новую базу при запуске
python app.py
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в консоли
2. Убедитесь в наличии всех файлов
3. Проверьте права доступа к файлам
4. Переустановите зависимости

## 📄 Лицензия

Проект создан в учебных целях.

## ✅ Чек-лист требований

- [x] Система полностью работает и запускается на macOS
- [x] Добавление сотрудников с ФИО, номером и должностью
- [x] Валидация данных сотрудников
- [x] Проверка на дубликаты сотрудников
- [x] Поиск по ID, ФИО и номеру телефона
- [x] Добавление курсов с названием, длительностью и документом
- [x] Валидация данных курсов
- [x] Регистрация на курсы
- [x] Жизненный цикл: зачислен → учится → завершил
- [x] Логика жизненного цикла (нельзя завершить если не поступил)
- [x] Написано на Python
- [x] Автоматические отчёты в CSV
- [x] Описательная статистика в отчётах
- [x] Автоматические советы по улучшению
- [x] Удаление записей с историей
- [x] Pytest модульное тестирование (35+ тестов)
- [x] Красивый визуальный дизайн фронтенда
- [x] Полная документация

---

**Создано**: 2025
**Версия**: 1.0
**Статус**: Готово к использованию ✅
